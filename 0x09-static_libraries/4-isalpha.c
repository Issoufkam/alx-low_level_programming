#include "main.h"
#include <ctype.h>
#include <string.h>
#include <stdlib.h>

/**
 * _isalpha - Print the letters
 * @c: The characters
 * Return: Nothing
 */
int _isalpha(int c)
{
	return (isalpha(c));
}
